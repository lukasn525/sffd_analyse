# Decision Log — Abweichungen vom Exposé und ihre Begründung

> **Lebensdauer dieses Dokuments:** wächst, wird nie umgeschrieben. Eine
> Entscheidung, die durch eine spätere überholt wird, bleibt stehen und bekommt
> im Status den Vermerk „überholt durch #x". Ergebniszahlen stehen in
> `03_STAND.md`, nicht hier — hier stehen nur die Zahlen, die eine Entscheidung
> **begründet haben**. Sie sind deshalb bewusst nicht auf den finalen Lauf vom
> 31.08.2026 nachgezogen, der nach der Korrektur der Wohnbevölkerung mit 36
> statt 35 Stadtteilen rechnet.

Jede Entscheidung, die vom Exposé abweicht oder eine methodische Festlegung
trifft, steht hier mit Datum, Grund und Status. Das ist die Grundlage für die
Sprechstunde bei Prof. Schröter und für Kapitel 6 der Arbeit.

**Hinweis zu Dateinamen:** Die Einträge nennen die Dateinamen, die zum jeweiligen
Zeitpunkt galten (`01_fetch.py`, `02_join.py`, `aggregation.py`, `cv.py`,
`s01_laden.py` …). Sie bleiben im Original stehen, weil sie dokumentieren, was
wann entschieden wurde. Der heutige Stand steht in `03_STAND.md`.

| # | Datum | Entscheidung / offener Punkt | Begründung | Status |
|---|---|---|---|---|
| 1 | 2026-07-18 | Aggregation Stadtteil×Monat als eigener Schritt in `modellierung/`, nicht in der Prep-Pipeline | Prep-Pipeline bleibt unverändert (Absprache); Exposé verlangt diese Ebene | umgesetzt |
| 2 | 2026-07-18 | Ridge auf log(1+y) statt roher Zählung | Linearitätsprüfung: y stark rechtsschief/heteroskedastisch; log-Baseline annähernd linear → Schröter-Vorgabe erfüllt, Ridge bleibt als interpretierbare Baseline im Vergleich | mit Schröter bestätigen; bestätigt durch #47 (08.08.2026); präzisiert durch #43 — log1p gilt seither für die **Rate**, nicht für die Zählung |
| 3 | 2026-07-18 | Zeitbewusste Crime-Aggregation (Kumulation bis Vorjahr) in 02_join implementiert; aktiv nach Crime-Neu-Download (Rohdatei ohne Datumsspalte); Land Use bleibt statisch (nur ein Snapshot) | Leakage-Behebung mit Zustimmung von Lukas; Fallback statisch dokumentiert | **Code umgesetzt; Lukas: `01_fetch.py` mit `DOWNLOAD_CRIME=True` neu laufen lassen**; Neu-Lauf erfolgt (`data/raw/crime_raw.parquet`, 26.07.2026) |
| 4 | 2026-07-18 | ACS-Join auf *letzten verfügbaren* Snapshot umgestellt (statt *nächsten*); 2003–2008 Rückgriff auf ACS 2009 | Strikte Prognose-Logik, kein Zukunfts-Leakage mehr; Linearitätsbefund bleibt stabil (R² 0,71) | **umgesetzt & validiert (2026-07-18)** |
| 5 | 2026-07-18 | **Hauptanalyse ab 2014** (nicht 2012): mit strikt prognostischem ACS-Join hängen 2012/13 am ACS 2009 (ohne B15003) → `akademikerquote_pct` erst ab 2014 verfügbar; Sensitivitätsanalyse voller Zeitraum ohne dieses Feature | Ehrliche NaN-Behandlung statt versteckter Imputation; Parameter `ab_jahr`/`ohne_ausreisser` in `aggregation.py` | entschieden; mit Schröter bestätigen; **überholt durch #18** (Analysezeitraum fest 2015-01 bis 2025-12) |
| 6 | 2026-07-18 | Klassifikation binär Brand vs. Nicht-Brand (statt Multiklasse) | Klassenverteilung stark unbalanciert (Brand 13,1 %); Exposé sieht Vereinfachung explizit vor | vorgesehen; **überholt durch #21** (vier Klassen statt binär) |
| 7 | 2026-07-18 | Dedup nach `einsatz_nummer` **in die Prep-Pipeline verlagert** (02_join, mit Zustimmung); Sicherheits-Dedup in `modellierung/aggregation.py` bleibt (idempotent) | Bereinigung gehört fachlich in die Data Preparation; validiert: 0 Duplikate im Output | umgesetzt & validiert |
| 8 | 2026-07-18 | Baseline-Problem **empirisch gelöst durch Option A**: Lag-Features (`lag_1`, `lag_12`, `rolling_mean_3`) für alle Modelle; Demo: Ridge (S+L) R² 0,91 und RF (S+L) 0,90 schlagen Naiv 0,88; Set S bleibt für Unterfrage 1 im Vergleich | Fair (identische Zeilen/Folds), praxisüblich; Erklärungsbeitrag der Strukturmerkmale wird über SHAP quantifiziert | umgesetzt in Demo; **Framing mit Schröter bestätigen**; **überholt durch #29** — die Lag-Merkmale sind seither **kein Modellmerkmal** mehr |
| 9 | 2026-07-18 | Lag-Features werden für Ridge log(1+x)-transformiert (modellinterne Aufbereitung analog Skalierung) | Rohe Lags in log-Zielgrößen-Modell fehlspezifiziert (empirisch R² −5,9); log-AR-Spezifikation korrekt | umgesetzt; **überholt durch #29** — betrifft Merkmale, die nicht mehr ins Modell eingehen |
| 10 | 2026-07-18 | **Audit-Fix:** `bfill` aus der Stadtteil-Monat-Aggregation entfernt (nur noch `ffill`) | `bfill` imputierte fehlende Werte (v. a. `akademikerquote_pct` vor 2014) stillschweigend mit **Zukunftswerten** (Leakage); nach Fix ehrliche NaN, Behandlung über Zeitraumfilter (#5); Demo-Ergebnisse bleiben robust (Ridge S+L 0,91 · RF S+L 0,90 · Naiv 0,88; Linearität R² 0,74) | umgesetzt & validiert |
| 11 | 2026-07-26 | **ACS-Publikationsversatz +1 Jahr** (`ACS_PUBLIKATIONS_LAG` in `02_join.py`): Snapshot-Bedingung jetzt `acs_jahr ≤ Einsatzjahr − 1` | Die ACS-5-Jahres-Schätzung für Jahr y erscheint erst ca. Dez. y+1. Ohne Versatz nutzte ein Einsatz aus 2023 den ACS-Jahrgang 2023 – zum Prognosezeitpunkt nicht publiziert, Modell nicht implementierbar. Kostet den Jahrgang 2014; **Hauptanalyse startet nun 2015** | **umgesetzt & validiert (Pipeline neu gerechnet)**; mit Schröter bestätigen |
| 12 | 2026-07-26 | **Audit-Fix: Randmonat-Konstante `ENDE = 202512`** in `aggregation.py` (statt „größter vorhandener `jahr_monat` minus 1") | Alte Logik schnitt nur 2026-02 (1 Einsatz) ab; **2026-01 blieb mit 258 statt ~3.300 Einsätzen als scheinbar vollständiger Monat im Panel** und lag im Testfenster des letzten Folds. Wirkung: naive Baseline dort R² 0,740 statt 0,955. Zusätzlich warnt `pruefe_randmonate()` bei künftigen Downloads | **umgesetzt & validiert** |
| 13 | 2026-07-26 | **Exposure: `log_bevoelkerung` statt roher `gesamtbevoelkerung`** als Prädiktor; Rohwert bleibt für NegBin-Offset und Raten-Sensitivität im Datensatz | Ohne Exposure-Kontrolle sagt das Modell im Kern die Stadtteilgröße vorher: `armutsquote_pct` r=+0,20 auf absolute Counts, aber **−0,13** auf Einsätze je 1.000 Ew.; `anteil_risikogewerbe_pct` +0,70 vs. −0,12. Zielgröße bleibt Zähldaten → NegBin/Overdispersion-Argumentation intakt. Empirischer Effekt: Ridge (S) R² 0,17 → **0,52** | umgesetzt; Raten-Sensitivität erledigt durch #29 (die Rate ist eigene Zielgröße); mit Schröter abgestimmt; Hinweis: der genannte Offset ist seit #45 der des **Poisson**-GLM, nicht der Negative Binomial |
| 14 | 2026-07-26 | **End-Hold-out der letzten 12 Monate** (`modellierung/cv.py`, `split_holdout`): 2025-01–2025-12 wird bei Modellwahl und Tuning nie berührt | Fold 3 war zugleich CV-Fold und letzter Zeitraum → beim Tuning wird zwangsläufig darauf geschaut. Zeitschnitte, inneres Validierungsfenster und Gütemaße jetzt zentral in `cv.py`, damit alle Verfahren konstruktiv identische Splits sehen (Fairness-Regel) | umgesetzt & validiert; **überholt durch #29** — der Zeitschnitt ist durch den Stadtteil-Split ersetzt; ein End-Hold-out der letzten 12 Monate existiert nicht |
| 15 | 2026-07-26 | **Balanciertes Panel: 38 statt 39 Stadtteile** (`balanciertes_panel()`); Mission Bay zusätzlich zu Treasure Island und Lakeshore ausgeschlossen | Mission Bay ist erst ab ACS 2021 als eigene Analyseeinheit enthalten. Zeilenweises `dropna` hätte ein **unbalanciertes** Panel erzeugt (Fold 1 mit 37, spätere Folds mit 38 Stadtteilen) → Testfenster-Summen springen allein durch den Zutritt eines Stadtteils. Rechteckiges Panel ist Voraussetzung für den fairen Fold-Vergleich | umgesetzt & validiert; weiter verdichtet durch #19 auf **35** Stadtteile |
| 16 | 2026-07-26 | ~~Crime-Merkmale vorerst statisch belassen~~ | – | **überholt durch #17** |
| 17 | 2026-07-26 | **Relativer Kriminalitätsindex je Stadtteil × Monat** ersetzt `anteil_gewaltdelikte_pct` und `anteil_eigentumsdelikte_pct`. Maß: **alle Straftaten je Einwohner, relativ zum Stadtdurchschnitt desselben Monats** (Location Quotient), rollierendes 12-Monats-Fenster endend im Vormonat. Quellen: `tmnf-yvry` (2014–2017, Spatial Join) + `e3si-785i` (ab 2018) | Die alten Merkmale waren (a) **statisch** (0 % Zeitvarianz), (b) über den gesamten Zeitraum inkl. Testfenster kumuliert (**Leakage**) und (c) Anteile statt Intensitäten – zwei Stadtteile mit sehr unterschiedlicher Deliktdichte konnten identische Werte haben. Der relative Index löst alle drei Punkte und ist zugleich robust gegen den SFPD-Systemwechsel 05/2018, weil sich ein stadtweiter Niveausprung im Quotienten kürzt. Konsistent mit der Exposure-Entscheidung #13 | **Code umgesetzt & Logik getestet; Lukas muss `01_fetch.py` mit `DOWNLOAD_CRIME=True` und `DOWNLOAD_CRIME_HISTORISCH=True` laufen lassen**; Neu-Lauf erfolgt (`data/raw/crime_historisch_raw.parquet`, 26.07.2026) |
| 21 | 2026-07-26 | **Klassifikation mehrklassig statt binär** (revidiert #6): Zielgröße `einsatzart_gruppe` mit **4 zusammengefassten NFIRS-Serien** – Fehlalarm/Good Intent 48,2 % · Technische Hilfe/Gefahr 24,0 % · Rettung/EMS 14,2 % · Brand 13,6 %. Metriken **Macro-F1 und Macro-AUROC (One-vs-Rest)**, Zuordnung über `argmax` statt Schwellenwert. Die binäre Zielgröße `ist_brand` bleibt als Robustheitslauf im selben Datensatz erhalten | (a) **Bessere Balance:** 3,6:1 statt 6,4:1 – die binäre Vereinfachung erzeugte das Ungleichgewicht, das sie vermeiden sollte. (b) **Binär verwirft das stärkste Struktursignal:** Fehlalarm-Anteil streut je Stadtteil 28,8–61,0 %, Technische Hilfe 11,6–44,9 % (Spannen > 30 pp, Ursache: automatische Brandmeldeanlagen in Hochhäusern/Gewerbe) – beide liegen binär in „Nicht-Brand" und sind unsichtbar; Brand selbst streut nur 20,7 pp. (c) **Exposé-treuer:** dort ist die Einsatzart „kategoriale Zielgröße", und „seltene Kategorien zusammenfassen" ist die zuerst genannte Option (S. 5). (d) Schwellenwert-Kalibrierung entfällt → Basisraten-Problem entschärft | umgesetzt in `modellierung/klassifikation_daten.py`; **mit Schröter bestätigen** |
| 20 | 2026-07-26 | **Klassifikation: Design festgelegt** (`docs/KLASSIFIKATION_DESIGN.md`). Ebene Einzeleinsatz, gleiche Abgrenzung wie die Regression (2015-01–2025-12, 35 Stadtteile, 350.481 Einsätze, 13,6 % Brand). **Ergebnisvariablen ausgeschlossen** (Sachschaden, Löschfahrzeuge/-kräfte, Alarmstufe, Antwortzeit – erst nach dem Einsatz bekannt). Ridge-Pendant = `LogisticRegression(penalty="l2")`. Schwellenwert je Fold auf dem inneren Fenster, **AUROC primär** | Die Basisrate verschiebt sich über den Zeitraum (Training 12,0 % → Test 17,3 % in Fold 1), weil die absolute Zahl der Brände 2019→2023 um 85 % stieg, bei nahezu konstanten Nicht-Brand-Einsätzen. AUROC ist davon unberührt, F1 nicht → Schwelle zeitnah kalibrieren, Anstieg als Befund in Kap. 6. Pseudo-Signal: 350.481 Zeilen enthalten nur **4.619 verschiedene Stadtteil-Monats-Profile** → SHAP nur blockweise, keine Signifikanztests auf Einsatz-Ebene | **überholt: Ebene durch #29 (Stadtteil × Monat statt Einzeleinsatz), Verfahrenswahl durch #31 (kein Ridge-Pendant mehr)** |
| 19 | 2026-07-26 | **Park-/Institutionsgebiete ausgeschlossen:** Golden Gate Park, Lincoln Park, McLaren Park (`PARKGEBIETE` in `aggregation.py`) → **35 Stadtteile, 4.620 Beobachtungen**. Zusätzlich geht der Kriminalitätsindex **logarithmiert** ins Modell (`log_kriminalitaetsindex`) | Gebiete ohne nennenswerte Wohnbevölkerung sind für ein bevölkerungsbezogenes Risikomodell keine sinnvolle Analyseeinheit: Golden Gate Park hat **45 Einwohner**, einen Kriminalitätsindex von 186 im Median und 7.394 Einsätze je 1.000 Ew./Jahr (Median aller übrigen: 14.435 Ew.). Dieser eine Stadtteil erzeugte eine Schiefe von 9,0 im Index und **Ridge (S) R² −3,9**. Nach Ausschluss + Log-Transformation: Schiefe 0,66, **Ridge (S) R² 0,79**. Entscheidung über die Analyseeinheit, keine Ausreißerbereinigung nach Zielgröße | umgesetzt & validiert; **mit Schröter bestätigen** |
| 18 | 2026-07-26 | **Analysezeitraum dauerhaft festgesetzt: 2015-01 bis 2025-12** (`START`/`ENDE`, nicht mehr aus den Daten abgeleitet) | Reproduzierbarkeit: Jeder Lauf liefert denselben Zeitraum, unabhängig davon, wie weit der letzte DataSF-Download reicht | umgesetzt & validiert |
| 22 | 2026-07-27 | **Restrukturierung: eine Aufbereitung, ein Befehl, zwei Datensätze.** `pipeline/`, `modellierung/` und `analyse/` sind aufgelöst; die gesamte Aufbereitung liegt in **`prep/`** (7 Dateien, `s01`–`s04` in Ausführungsreihenfolge), die Modellskripte in **`modelle/`** (3 Dateien). `python prep/build.py` erzeugt `regression.parquet` und `klassifikation.parquet` und prüft anschließend die Verfahrenseignung. Alle Konstanten stehen in **`prep/config.py`** (vorher über vier Dateien verteilt, `ACS_YEARS` sogar doppelt). Neu: Die CV-Aufteilung wird als Spalten `fold` und `ist_holdout` in die Datensätze geschrieben. Gestrichen: `deskriptiv.py`, `dashboard.py`, `sf_fire_incidents_base.parquet`, `sf_fire_risk_features_cleaned.parquet` | Die Aufbereitung war über zwei Ordner verteilt, mit einer Namenskollision (`03_features.py` vs. `features.py`), fünf Aufrufen in fester Reihenfolge und einem finalen Datensatz, der nur im Speicher existierte. **Der Umbau ist inhaltlich folgenlos:** Vor Aktivierung des Lag-Vorlaufs war der neue Datensatz zellengleich zum alten (`pd.testing.assert_frame_equal` bestanden, Nachweis in `docs/UMBAU_PREPROCESSING.md` Schritt 4). Die Fold-Spalten sichern die Fairness-Regel konstruktiv ab | umgesetzt & validiert (14/14 Prüfungen); **Schröter gegenüber erwähnen** |
| 24 | 2026-07-27 | **Datentypen der Merkmale auf `float64` vereinheitlicht** und die beiden Datensatz-Skripte zu `prep/s2_datensaetze.py` zusammengelegt (7 statt 9 Dateien in `prep/`) | Die ACS-Aggregation lieferte `median_haushaltseinkommen` und `median_miete` als pandas-eigenen Typ `Int64`. Beide sind **Modellprädiktoren**. Folge: `X.to_numpy()` ergab ein **object**-Array statt float64. scikit-learn fing das still ab (Ridge und Random Forest liefen fehlerfrei), **XGBoost lehnt solche DataFrames jedoch ab** – der Fehler wäre erst beim dritten der drei zu vergleichenden Verfahren aufgetreten und hätte im Preprocessing gesessen. Zusätzlich waren `jahr`/`monat` in den beiden Datensätzen unterschiedlich typisiert (int32 vs. int64). Nur die Typen ändern sich, kein einziger Wert (nachgerechnet). Die Zusammenlegung beseitigt zugleich die Reihenfolgeabhängigkeit, dass die Klassifikation den Regressionsdatensatz von der Platte lesen musste | umgesetzt & validiert (14/14 Prüfungen, neuer Test `test_datentypen_modelltauglich`) |
| 25 | 2026-07-27 | **`prep/` von sieben auf vier Dateien verdichtet.** `s01_laden.py` + `s02_einsaetze.py` → **`s1_daten.py`**; `s03_datensaetze.py` + `cv.py` → **`s2_datensaetze.py`**; `s04_eignungspruefung.py` + `modelle/m01_baselines.py` → **`s3_pruefung.py`**. Die ersetzten Dateien liegen unverändert in `prep/_archiv/`. `config.py` und `build.py` bleiben unberührt | Der Schnitt folgte der Entstehungsgeschichte, nicht der Sache: Download und Join sind ein Arbeitsschritt, nicht zwei; die Zeitschnitte gehören zum Datensatz, weil sie als Spalten `fold`/`ist_holdout` in die Parquet-Datei geschrieben werden; und die Baselines schätzen nichts, was getunt wird – sie legen die Latte fest, über die die Verfahren springen müssen, und gehören damit zur Abnahme der Aufbereitung, nicht zur Modellierung. **Inhaltlich folgenlos:** `einsaetze.parquet`, `regression.parquet`, `klassifikation.parquet` und beide `baselines_*.csv` sind nach dem Umbau **byte-identisch** (MD5 verglichen). Preis: `modelle/m02` und `m03` importieren ihre Gütemaße jetzt aus `s2_datensaetze` statt aus `cv` | umgesetzt & validiert (14/14 Prüfungen, alle Ausgaben byte-identisch) |
| 30 | 2026-07-28 | **Fold-Zuteilung zusätzlich nach der seltensten Klasse stratifiziert.** Sortiert wird jetzt nach der Anzahl brand-dominierter Monate, bei Gleichstand nach Bevölkerung. Die Zuteilung erfolgt einmal in `run()` für beide Datensätze; `stadtteil_gruppen` und `ergaenze_aufteilung` sind zu einer Funktion zusammengefasst | Von 70 brand-dominierten Monaten liegen 35 allein in Bayview Hunters Point, weitere 20 in Bernal Heights und Portola — die Klasse kommt in nur 10 von 35 Stadtteilen vor. Ohne diese Stratifizierung hatte in **drei von vier Aufteilungen ein Fold null Brand-Testfälle**; Macro-F1 mittelt dann über eine Klasse, die im Test gar nicht existiert, und springt zwischen den Folds. Da 25 Stadtteile gar keine Brand-Monate haben, bleibt die Größenstratifizierung für den Großteil erhalten. Kein Leakage: Die Gruppenbildung gibt dem Modell keine Information, sie legt nur fest, welche Stadtteile gemeinsam getestet werden — Standardvorgehen, vgl. `StratifiedGroupKFold`. Ergebnis: alle Folds haben jetzt 2 bis 13 Brand-Testfälle statt 0 bis 42 | umgesetzt & validiert (19/19 Prüfungen) |
| 29 | 2026-07-28 | **Stadtteil-Split statt Zeitschnitt, und Klassifikation auf Stadtteil × Monat.** Drei zusammenhängende Änderungen: (1) `fold`/`ist_holdout` teilen jetzt **Stadtteile** statt Monate — 5 Folds à 6 Stadtteile plus 6 Hold-out-Stadtteile, stratifiziert nach Bevölkerung. (2) Die Klassifikation liegt nicht mehr auf Einzeleinsatz-Ebene, sondern misst die **Zusammensetzung** je Stadtteil × Monat (vier NFIRS-Anteile). (3) Die **Rate** (Einsätze je 1.000 Einwohner) kommt als zweite Zielgröße dazu; die Lags bleiben im Datensatz, sind aber kein Modellmerkmal mehr | Die Forschungsfrage lautet: Lässt sich aus Strukturmerkmalen vorhersagen, wie viele und welche Einsätze ein Stadtteil hat? Ein Zeitschnitt prüft das **nie** — dort steht jeder Stadtteil in Training und Test, und `lag_1` liefert sein Niveau frei Haus. Nachgerechnet: 92,5 % der Varianz liegen zwischen den Stadtteilen, der Stadtteil-Mittelwert allein erreicht R² 0,888. Beim Stadtteil-Split müssen die Strukturmerkmale tatsächlich erklären — Ridge erreicht für einen **unbekannten** Stadtteil R² 0,647 auf der Rate und 0,584 auf der absoluten Zahl. Die Einzeleinsatz-Klassifikation hatte eine harte Obergrenze von 49,9 % gegenüber 48,2 % für bloßes Raten; auf Stadtteilebene ist der Fehlalarm-Anteil dagegen mit R² 0,66 vorhersagbar. **Abweichung vom Exposé:** Unterfrage 2 nennt zeitreihengerechte Kreuzvalidierung — die entfällt als Hauptvergleich und wird zur Nebenbemerkung | umgesetzt & validiert (17/17 Prüfungen); **mit Schröter besprechen**. **Präzisiert am 04.08.2026:** „Nebenbemerkung" heißt **nicht gerechnet**, sondern in Kapitel 8 begründet verworfen. Ein zweiter Validierungsrahmen wäre ein zweiter Analysestrang und verstieße gegen R1 (ein einheitlicher Rahmen) und R8 (Fokus). Der Beleg liegt ohnehin vor: Der Stadtteil-Mittelwert allein erklärt R² 0,925, ein Zeitschnitt würde also im Kern die zeitliche Stabilität messen. Die Lag-Spalten bleiben im Datensatz, aber als **Deskription** der zeitlichen Struktur (Autokorrelation), nicht als Vorrat für einen Zusatzlauf |
| 31 | 2026-08-03 | **Klassifikation nur mit Random Forest und XGBoost; logistische Regression gestrichen** (revidiert die Festlegung in `04_MODELLIERUNG.md` §3). Die Klassifikationsmenge ist damit eine **echte Teilmenge** der Regressionsmenge: drei Verfahren für die Menge (Ridge, RF, XGBoost), zwei für die Struktur (RF, XGBoost). Kein Verfahren kommt hinzu, genau eines fällt weg | Freigabe Schröter per E-Mail 03.08.2026: „Drei Algorithmen für Regression und zwei für Klassifikation sind unproblematisch, solange die Auswahl fachlich nachvollziehbar ist" — mit der Auflage, die unterschiedliche Auswahl in 6.2 methodisch zu begründen. **Warum RF und XGBoost sich übertragen:** Zwischen Regression und Klassifikation wechselt bei ihnen ausschließlich die Verlustfunktion (Gini/Entropie statt Varianzreduktion beim Split; `multi:softprob` statt `reg:squarederror` als Objective). Der Ensemble-Mechanismus bleibt identisch, beide sind nativ mehrklassenfähig. **Warum Ridge sich nicht überträgt:** Ridge minimiert den quadratischen Fehler auf einer metrischen Zielgröße; `dominante_einsatzart` ist nominal mit vier ungeordneten Klassen, es gibt weder Ordnung noch Abstand, der quadratische Fehler ist dort undefiniert. `RidgeClassifier` wurde geprüft und verworfen (±1-Kodierung, One-vs-Rest, keine kalibrierten Wahrscheinlichkeiten). **Warum die logistische Regression trotz funktionierendem Vortest entfällt:** Sie erreichte Macro-F1 0,282 gegen 0,301 (RF) und 0,220 (Mehrheitsklasse) — sie funktioniert, wäre aber ein vierter Verfahrensstrang. Bei zwei Zielgrößen ist genau diese Verbreiterung das unter R4 benannte größte Notenrisiko; R8 verlangt Fokus. Das Teilmengenprinzip erzeugt dabei **keine** heterogenen Spezifikationen (R1): beide Zielgrößen laufen durch denselben Preprocessing- und Validierungsrahmen und werden von denselben Modellfamilien bearbeitet | entschieden; **Begründung gehört nach 6.2 (Auflage Schröter)** |
| 32 | 2026-08-03 | **Baselines festgelegt: Negative Binomial für die Regression, Mehrheitsklasse für die Klassifikation.** Gestrichen: naiver Vormonatswert (mit #29 ohnehin entfallen), saisonaler Durchschnitt, die vier `anteil_*`-Baselines. Der Gesamtmittelwert bleibt als Nullmarke, nicht als Gegner. Die Rate erhält ihre NegBin-Referenz aus derselben Vorhersage, geteilt durch die Bevölkerung — ein zweites Modell wäre eine zweite Spezifikation. `prep/s3_baselines.py` von 196 auf 150 Zeilen | Ein Gesamtmittelwert sagt für Seacliff (6,4 Einsätze/Monat) und Tenderloin (279,7) dieselbe Zahl — als Gegner zu leicht, ein Sieg darüber beweist nur, dass die Merkmale nicht wertlos sind. Nachgerechnet fielen Gesamtmittelwert und saisonaler Durchschnitt zudem praktisch zusammen (RMSE 71,19 vs. 71,09), weil unter dem Stadtteil-Split 92,5 % der Varianz zwischen den Stadtteilen liegen: faktisch eine triviale Referenz, nicht zwei. Die **Negative Binomial** ist dagegen ein vollwertiges Modell mit denselben zwölf Merkmalen, denselben Zeilen und denselben Folds, über den Log-Link auf der Originalskala nichtlinear und verteilungsgerecht für Zähldaten. Entscheidend ist, welche Trennlinie sie zieht: Sie kann Krümmung, aber **keine Wechselwirkungen** zwischen Merkmalen — genau das, was RF und XGBoost konstruktionsbedingt finden. Damit wird der Vergleich zum Beleg statt zur Behauptung (Auflage Schröter 27.07.: „bei nichtlinearen Zusammenhängen braucht es eine nichtlineare Baseline"). Die `anteil_*`-Baselines fielen weg, weil die Anteile laut `04_MODELLIERUNG.md` §1 keine Modellzielgröße sind — Referenzwerte für etwas, das niemand modelliert (R8). **Offen zu benennen:** In der Klassifikation gibt es kein NegBin-Pendant, weil eine Zahl keine von vier ungeordneten Kategorien vorhersagen kann; dort bleibt die Mehrheitsklasse die einzige Referenz und die Latte liegt niedriger als in der Regression | **Baselinewahl überholt durch #45** (Poisson statt Negative Binomial); die Begründung der zweistufigen Konstruktion bleibt gültig. Werte in `03_STAND.md` |
| 33 | 2026-08-04 | **Drei Arbeitsschritte, drei Ordner — und die logistische Regression wird Baseline der Klassifikation.** Neuer Ordner `vorpruefung/` mit `v1_baselines.py` (aus `prep/s3_baselines.py`) und `v2_eignung.py` (aus `modelle/m01_eignung.py`). Die Baselines laufen jetzt in **zwei Stufen**: Stufe 1 ohne Merkmale (Gesamtmittelwert, Mehrheitsklasse), Stufe 2 mit denselben Merkmalen in der einfachsten zur Datenform passenden Form (Negative Binomial, **multinomiale logistische Regression**). `v2_eignung.py` liest die Baseline-Werte, statt sie neu zu rechnen | Der Zuschnitt folgt jetzt der Frage, die ein Schritt beantwortet: `prep/` erzeugt Daten, `vorpruefung/` legt fest, was ein Modell mindestens leisten muss und welche Verfahrensklasse überhaupt passt, `modelle/` vergleicht. Vorher lagen Baselines in `prep/` (Auflage Schröter) und die Eignungsprüfung in `modelle/` — zwei Teile **eines** Arguments in zwei Ordnern. Die Auflage bleibt gewahrt: Die Baselines stehen weiterhin vor der Modellierung und werden in Kapitel 5 berichtet. **Die logistische Regression als Stufe 2** löst zugleich zwei offene Punkte: Der Klassifikation fehlte bislang eine merkmalsbasierte Referenz — es gab nur die Mehrheitsklasse (trivial) und dann direkt die Baumverfahren. Und die in #31 aus Fokusgründen gestrichene logistische Regression erreichte im Vortest mit Macro-F1 0,290 den besten Wert der drei geprüften Verfahren; als Messlatte statt als viertes Vergleichsverfahren wird dieser Wert vom Problem zum Beweismittel. Die Struktur ist damit in beiden Strängen symmetrisch | umgesetzt und gelaufen; **Logit-Variante überholt durch #45** (ohne Strafterm). Werte in `03_STAND.md` |
| 34 | 2026-08-04 | **Die Forschungsfrage wird über drei einzeln messbare Bausteine beantwortet, nicht über eine Rangfolge zwischen den Verfahren.** Primäraussage ist **je Verfahren gegen die Stufe-2-Baseline** (UF2), dazu **Trainings- und Inferenzaufwand** (UF3), daraus die **Eignungsaussage für diesen Datensatz** (UF4). Eine Rangfolge *zwischen* den Verfahren wird nur berichtet, wenn der gepaarte Wilcoxon-Test sie hergibt (α = 0,05, je Zielgröße getrennt); andernfalls steht dort „nicht unterscheidbar" mit Konfidenzintervall der Differenz. **Verworfen:** getrennte Auswertung nach Extrapolationsgrad | Der Abstand Verfahren↔Baseline ist ein Vielfaches des Abstands Verfahren↔Verfahren. Im Vortest lag die gepaarte Differenz Ridge gegen Random Forest bei +0,042 ± 0,609 — bei 29 Entwicklungsstadtteilen nicht von null zu trennen (R-1). Der Abstand zur Baseline ist dagegen messbar. **Die beantwortbare Version der Frage steht bereits im Exposé:** Unterfrage 2 lautet wörtlich „im Vergleich zu **Baselines**", nicht „im Vergleich untereinander". Unterfrage 3 ist gegen R-1 zudem immun — im Aufwand unterscheiden sich die Verfahren um Größenordnungen, das ist unbestreitbar messbar. Und Unterfrage 4 verlangt ohnehin eine Aussage über *diesen* Datensatz, nicht eine allgemeine Rangfolge. Damit hört R-1 auf, existenziell zu sein: Es bedroht die Rangfolge, nicht die Arbeit. Die getrennte Auswertung nach Extrapolationsgrad hätte differenziert, weicht aber die **Fairness-Regel** auf — die Verfahren würden dann nicht mehr auf identischen Zeilen verglichen. Der Extrapolationsanteil bleibt Limitation. **Entscheidend: Diese Festlegung steht VOR dem Rechnen.** Sonst sucht man hinterher die Auswertung, die einen Unterschied zeigt | festgelegt am 04.08.2026, vor dem ersten Modelllauf |
| 57 | 2026-08-31 | **Der Fairnessbefund wird berichtet, wie er ist — als Zielkonflikt, nicht als Entwarnung.** R-24 und Kapitel 8.3 sagten bis heute, relativ zum Einsatzniveau bleibe „nichts Signifikantes". Auf dem finalen Lauf sind **drei von acht** Kombinationen auf 5 % signifikant: Poisson-GLM in beiden Zielgrößen (Rho +0,604 und +0,409) und Ridge bei der Anzahl (+0,298); beide Baumverfahren nicht. Der alte Wortlaut wird ersetzt, nicht relativiert | Die Prüfung war von Anfang an als *Prüfung* angelegt, nicht als Beleg — ein Ergebnis nur dann zu berichten, wenn es entlastet, wäre genau die ergebnisgetriebene Auswahl, die #34 und #52 ausschließen. Der Befund ist inhaltlich stark: Das sparsamste Modell prognostiziert im Mittel am besten und verteilt seinen Fehler am ungleichsten. Das ist ein Zielkonflikt zwischen Güte und Fehlergleichheit und gehört als solcher ins Ethikkapitel. **Zu benennende Grenze:** Der relative Fehler liegt über alle acht Kombinationen bei 0,401 bis 0,504 — die Schieflage sitzt auf einem durchweg hohen Fehlerniveau, es gibt keine Gruppe, für die das Modell brauchbar prognostiziert. Quelle: `results/fairness/fairness.csv` | umgesetzt 31.08.2026 in `06_RISIKEN.md` R-24 und `main.tex` §8.3 |
| 56 | 2026-08-31 | **Frage 2 der Suchdiagnose wird nicht berichtet — der Test ist eine Tautologie.** `im_alten_raum()` (`tools/suchdiagnose.py`, Z. 185) vergleicht jede Ziehung gegen `SUCHRAEUME` aus `config_modelle.py`, also gegen den **heutigen** Raum. Seit #49 ist der dort hinterlegte Vergleichsraum mit dem aktuellen identisch; alle 2.500 Ziehungen tragen deshalb `im_alten_raum = 1`. Die Funktion bleibt im Code stehen, der Wert wird nirgends zitiert | Der Defekt ist belegt statt vermutet: Ziehung 2 von menge/ridge/Fold 1 trägt `alpha = 32.147` bei einer alten Obergrenze von 1.000 und wird trotzdem als „im alten Raum" gezählt. **Was das nicht berührt:** Frage 1 (Budget 50 → 100) liest allein `bester_bisher` und ist gültig. **Was das nicht berührt:** Entscheidung #49 selbst — sie wurde am 13.08.2026 gemessen, als `SUCHRAEUME` noch die alten Räume hielt, und die Randkontakte sind unabhängig davon direkt aus `tuning.csv` nachrechenbar. Löschen statt Stehenlassen wäre falsch: Die Funktion dokumentiert, was geprüft werden sollte, und der Kommentar sagt jetzt, warum sie es nicht kann | festgestellt 31.08.2026; im Code vermerkt, in `main.tex` nicht verwendet |
| 55 | 2026-08-31 | **Schalter `--weiter` in `m02_menge.py` und `m03_struktur.py`.** Übernimmt `tuning.csv` und `*_folds.csv` eines bereits gelaufenen Tunings, statt es zu wiederholen. **Nur mit ausdrücklichem Schalter** — ohne ihn wird immer neu gerechnet. Fünf Bedingungen müssen erfüllt sein, sonst bricht der Lauf ab: `config_modelle.py` und die Parquet-Datei sind nicht jünger als `tuning.csv`, die Foldzahl entspricht `ZIELE × VERFAHREN × WIEDERHOLUNGEN × N_FOLDS`, `n_train + n_test` entspricht der Panelgröße, die Verfahren stimmen überein, und die Foldnummern sind vollständig | **Der Docstring von `phase_tuning` verbot ausdrücklich die stille Wiederverwendung** — aus gutem Grund: Ein übernommenes Tuning zu einer veränderten Datenlage ist genau die Fehlerklasse, die den Neulauf vom 29.08. nötig gemacht hat. Der Schalter hebt das Verbot nicht auf, er macht eine **benannte** Ausnahme daraus, und der Docstring sagt das jetzt so. Anlass: derselbe Datenstand am selben Tag zweimal zu tunen kostet 139 Minuten ohne jeden Erkenntnisgewinn. **Warum Guards und nicht Vertrauen:** Die einzige Gefahr des Schalters ist, dass er *unbemerkt* auf veränderte Daten trifft; jede der fünf Bedingungen prüft genau eine Art, wie sich das äußern würde. Ein Abbruch ist billig, ein still falsches Tuning nicht | umgesetzt 31.08.2026, auf Wunsch des Verfassers gegen meine Empfehlung — die Guards sind der Preis dafür |
| 54 | 2026-08-30 | **Der Lauf vom 30./31.08.2026 ist die berichtete Hauptanalyse; der Lauf vom 16.08.2026 entfällt vollständig.** Kein Vorher-Nachher-Vergleich, keine zweite Ergebnisreihe. Sämtliche Zahlen in `main.tex`, `03_STAND.md` und `results/` stammen aus diesem einen Lauf | Das ist der **technische** Ausnahmefall, den #52 vorab definiert hat: „wenn die neue Konfiguration einen nachweisbaren Fehler enthält". Der Fehler ist nachgewiesen (#53), nicht vermutet, und er betrifft die Exposition jeder einzelnen Zeile — der alte Lauf ließe sich nur unter Hinweis auf einen bekannten und behobenen Defekt berichten. Dass die Zahlen sich dabei teils verschlechtern (der Extrapolationsanteil steigt von 34,6 auf 36,6 %, der Hold-out-Vorsprung schrumpft von 7,6 auf 34,8 % Abstand), ist kein Argument in die eine oder andere Richtung — die Regel aus #52 stellt gerade sicher, dass die Richtung der Zahlen nicht entscheidet | festgelegt 30.08.2026, **bevor die neuen Ergebnisse vorlagen** |
| 53 | 2026-08-29 | **Basiscode-Fallback in `tract_zu_stadtteil()` und Abbruchschwelle `assert quote >= 0.95`.** Findet die volle elfstellige GEOID eines Census Tracts keinen Partner im Crosswalk, greift der vierstellige Basiscode — aber nur, wenn er im Crosswalk **eindeutig** auf einen Stadtteil zeigt; uneindeutige Basiscodes bleiben unzugeordnet. Unmittelbar danach bricht der Aufbau ab, wenn weniger als 95 % der Wohnbevölkerung eines Jahrgangs zugeordnet sind. Folge: **Mission Bay** hat wieder eine Wohnbevölkerung, der Analysezuschnitt steigt von 35 auf **36 Stadtteile**, und alle Modelle wurden neu gerechnet | **Der Fehler war still und er war groß.** Die Census-Tract-Grenzen ändern sich zwischen den ACS-Jahrgängen; über die volle GEOID fanden im Jahrgang 2014 nur 73,1 % und in 2019 nur 72,1 % der Wohnbevölkerung einen Stadtteil (2021/23: 100,0 %). Genau diese beiden Jahrgänge tragen die Merkmale für 2015 bis 2020. Über ein Viertel der Bevölkerung fiel dort aus der Exposition — der Offset des Poisson-GLM und jede Pro-Kopf-Größe rechneten mit einer zu kleinen Bezugsmenge, ohne dass irgendetwas fehlschlug. `06_RISIKEN.md` R-8 hatte das Problem seit dem 17.08. notiert und als „für die Hauptanalyse ab 2015 folgenlos" abgetan; das war falsch und hat den Fehler gedeckt. **Warum eine Zusicherung und nicht nur ein Fix:** Der Fallback behebt den bekannten Fall; die Schwelle fängt den nächsten ab, den niemand vorhergesehen hat. Ein Abbruch beim Aufbau ist die einzige Stelle, an der eine zu kleine Exposition noch auffällt — danach sieht sie wie eine gültige Zahl aus. **Zu benennende Grenze:** Der Fallback unterstellt, dass ein neu geschnittener Tract im selben Stadtteil liegt wie sein Vorgänger. Für die 39 betroffenen Tracts von 2014 und 2019 trifft das zu; als Verfahren bleibt es eine Setzung und gehört in die Limitationen | umgesetzt 29.08.2026 in `prep/s1_daten.py`; löst den vollständigen Neulauf aus (#54) |
| 52 | 2026-08-14 | **Der Lauf mit Budget 100 ist die Hauptanalyse — festgelegt, bevor seine Zahlen existieren.** Berichtet wird ausschließlich der Lauf vom 16.08.2026 mit `TUNING_BUDGET = 100` und den Suchräumen nach #49. Der Lauf vom 07.08. bleibt als `archiv/2026-08-14_budget50` erhalten, erscheint in der Arbeit aber **nicht als zweite Ergebnisreihe** — auch nicht als Vorher-Nachher-Vergleich. **Ausnahme, vorab festgelegt:** Der alte Lauf kehrt nur aus **technischen** Gründen zurück — wenn ein erheblicher Teil der Anpassungen nicht konvergiert oder die neue Konfiguration einen nachweisbaren Fehler enthält. **Nicht**, weil die Zahlen ungünstiger ausfallen | **Warum überhaupt eine Regel:** Zwei Läufe zu haben und erst danach zu wählen, wäre eine ergebnisgetriebene Auswahl — dieselbe Fehlerklasse, die #34 für die Primäraussage ausschließt. Die Festlegung steht deshalb vor dem Lauf. **Warum der neue:** Beide Budgets sind belegbar (Bergstra & Bengio 2012 für 100, dieselbe Quelle mit 92,3 % Abdeckung für 50), die **alten Suchräume** dagegen nicht. In sechs von sieben geprüften Parametern lag mindestens ein Fold-Sieger an oder jenseits der Grenze; in der Klassifikation wählte XGBoost in **vier von fünf Folds die kleinstmögliche Baumtiefe** (#49). Das ist ein Befund ohne Gegenargument, und er liegt dokumentiert im Repo — der alte Lauf ließe sich nur unter Hinweis auf einen bekannten und nicht behobenen Defekt berichten. **Was das für den Text heißt:** Die Begründung der erweiterten Suchräume bleibt in Kapitel 6 und stützt sich auf die *gewählten Hyperparameter* der Vorabprüfung, nicht auf deren Gütemaße — es entsteht also kein Eindruck zweier Analysen. Der Überanpassungsnachweis (#51) ruht allein auf dem Abstand zwischen Trainings- und Testgüte **innerhalb** des neuen Laufs; ein Vorher-Nachher-Vergleich wird bewusst nicht berichtet, weil er mehr Fragen aufwürfe, als er beantwortet (R8: Fokus statt Breite). Das Archiv dient der Nachvollziehbarkeit und der internen Prüfung, nicht der Darstellung | festgelegt 14.08.2026, **vor dem Lauf** |
| 51 | 2026-08-14 | **Trainingsgüte wird je Lauf mitgemessen.** `m02_menge.ein_lauf` liefert zusätzlich `RMSE_train` und `R2_train`, `m03_struktur.ein_lauf` zusätzlich `macro_f1_train` und `accuracy_train`; die Aggregate tragen den Abstand als `ueberanpassung_*`. Kein zweiter Fit, nur eine weitere Vorhersage auf den Trainingsstadtteilen, **nach** der Zeitmessung — Unterfrage 3 bleibt unberührt | Der Strukturstrang zeigt die Signatur von Überanpassung: Beide Baumverfahren schlagen die Baseline in der Kreuzvalidierung und verlieren auf dem Hold-out, während die Baseline dort **besser** wird (0,297 → 0,327) und die Mehrheitsklasse etwa gleich bleibt (0,223 → 0,208). Wären die sechs Hold-out-Stadtteile schlicht schwerer, müssten alle Modelle einbrechen. Sie tun es nicht — und die Bäume trainieren dort sogar auf 29 statt 23 Stadtteilen, also mit **mehr** Daten. Bislang war das eine Auslegung; der Abstand zwischen Trainings- und Testgüte macht eine Messung daraus. Erste Stichprobe (Fold 1, Random Forest, alte Konfiguration): Menge R² 0,981 im Training gegen 0,656 im Test, Struktur Macro-F1 0,533 gegen 0,378. **Zu benennende Grenze:** Ein Wald mit `min_samples_leaf = 1` interpoliert seine Trainingsdaten konstruktionsbedingt — ein hohes Trainings-R² ist dort erwartbar und belegt für sich genommen keine krankhafte Überanpassung. Der Abstand ist deshalb *nicht* als Verhältnis zwischen Verfahren zu lesen, sondern zwischen Konfigurationen desselben Verfahrens (vor und nach #49) und als Größenordnung gegen die linearen Modelle. Der saubere Wert für Bäume wäre die Out-of-Bag-Schätzung; sie gibt es nur beim Random Forest und wäre gegenüber Ridge und XGBoost asymmetrisch — bewusst nicht erhoben, im Code benannt | umgesetzt 14.08.2026, **vor dem Lauf**; gehört in Kapitel 7 und in die Diskussion von R-2 |
| 50 | 2026-08-13 | **Tuning-Budget von 50 auf 100 Ziehungen, für alle Verfahren und beide Stränge.** `config_modelle.TUNING_BUDGET`. Die Suchräume bleiben je Verfahren verschieden, das Budget ist für alle gleich | **Hergeleitet statt gewählt.** Bergstra und Bengio (2012, S. 296) geben die geschlossene Form 1 − (1 − v/V)^T für die Wahrscheinlichkeit an, mit T Zufallsziehungen mindestens einmal in einen Zielbereich vom relativen Volumen v/V zu treffen. Für v/V = 0,05 steigt sie von **92,3 % bei 50** auf **99,4 % bei 100**. Der Ausdruck enthält die Dimension des Suchraums **nicht** — deshalb erhalten Ridge mit einem und XGBoost mit sieben Hyperparametern dasselbe Budget, ohne dass dem höherdimensionalen Verfahren ein Nachteil entsteht. Das ist zugleich die Antwort auf den naheliegenden Einwand, XGBoost bräuchte mehr Ziehungen. **Die Zahlen 92,3 und 99,4 sind unsere Anwendung ihrer Formel, nicht ihre Aussage** — die verbreitete Angabe „60 Ziehungen für das beste Fünf-Prozent-Fenster" steht nicht in dem Papier, geprüft am 13.08.2026 im Volltext. **Der eigentliche Anlass ist #49:** Ein weiterer Suchraum verdünnt die gute Region, dieselbe Zahl Ziehungen deckt ihn schlechter ab. Wer den Raum öffnet, muss das Budget mitziehen. **Gemessen** (`tools/suchdiagnose.py`, 13.08.): Die Verdopplung allein ist bei **vier von fünf** Verfahren wirkungslos — Random Forest im Mengenstrang gewinnt auf vier Nachkommastellen exakt null, Ridge +0,0065, beide Strukturverfahren +0,0001. Nur XGBoost in der Regression gewinnt spürbar (+0,1496 RMSE auf der Rate). Das gehört so in Kapitel 6: für ein Verfahren belegt, für vier ohne Wirkung. Erfüllt zugleich die Auflage vom 10.08. („Hyperparameteroptimierung … keine AUSREDE"). **Preis:** Die Tuningphase steigt von 66 auf 139 Minuten | entschieden 13.08.2026, **vor dem Lauf**; Umsetzung in `config_modelle.py` |
| 49 | 2026-08-13 | **Vier Suchräume erweitert, weil das Optimum an ihrer Grenze lag.** `alpha` (Ridge) 1e−3…1e3 → **1e−5…1e5**; `max_depth` (Random Forest) [None, 8, 12, 16, 24] → **[8, 12, 16, 24, 32, 48, None]**; `max_depth` (XGBoost) 3…10 → **1…14**; `reg_lambda` (XGBoost) 1e−2…1e2 → **1e−4…1e4**. **Nicht** erweitert: `max_features`, `min_samples_leaf`, `subsample`, `colsample_bytree` — deren Grenzen sind natürlich (alle Merkmale, eine Beobachtung, der ganze Datensatz), dahinter existiert nichts. Ebenfalls nicht: `n_estimators`, nur ein Fold lag nahe der Grenze und es ist der größte Laufzeittreiber | Gemessen mit `tools/suchdiagnose.py` am 13.08.2026, Budget 100, ausschließlich auf Entwicklungsstadtteilen, das Hold-out unberührt. In **sechs von sieben** geprüften Parametern liegt mindestens ein Fold-Sieger außerhalb der alten Grenze. **Der deutlichste Fall ist zugleich der wichtigste:** Bei `max_depth` von XGBoost im Strukturstrang wählten am 07.08. **vier von fünf Folds den Wert 3 — die Untergrenze**. Mit geöffneter Grenze wählen sie **2, 2 und 1**, ein weiterer 14. Dasselbe bei `reg_lambda`: zwei Folds gehen auf 3900 und 570 gegen eine alte Obergrenze von 100. Flachere Bäume **und** stärkere Regularisierung — das Modell wollte einfacher sein, als der Suchraum erlaubte. **Zusammenhang mit R-2 und B-42:** Im Strukturstrang schlagen beide Baumverfahren die Baseline in der Kreuzvalidierung und verlieren auf dem Hold-out, während die Baseline dort **besser** wird (0,297 → 0,327) — die Signatur von Überanpassung, nicht von schwierigen Testdaten. Wenn der Suchraum zusätzliche Komplexität erzwang, war ein Teil dieser Überanpassung auferlegt. **Die Begründung ist eine Aussage über die Suche, nicht über das Ergebnis:** Das Optimum lag an oder jenseits der Grenze, also war die Grenze falsch gesetzt. Nebenbefund: In der alten Liste stand `None` (unbegrenzte Tiefe) an **erster** Stelle, obwohl es faktisch der tiefste Wert ist — jede Auswertung, die die Listenposition als Tiefe liest, bekam ein verdrehtes Bild. Betraf auch Abbildung A8 | entschieden 13.08.2026, **vor dem Lauf**; Umsetzung in `config_modelle.py` |
| 48 | 2026-08-08 | **Die getunte Klassifikationsbaseline wird nicht nachgerechnet.** Die Stufe-2-Baseline der Struktur bleibt das unpenalisierte Logit (Macro-F1 0,297). Eine zusätzliche Sensitivität mit optimal eingestelltem Strafterm (0,314, gemessen am 06.08., `07_BEFUNDE.md` B-36) wird **nicht** erhoben | Der Punkt war real: Random Forest und XGBoost treten getunt gegen eine ungetunte Messlatte an, ihr Vorsprung von +0,0304 und +0,0371 fiele gegen die getunte Fassung auf +0,0136 und +0,0203 und wäre womöglich nicht mehr signifikant. Drei Gründe für das Verwerfen: (1) Schröter hat die unpenalisierte Form am 08.08. ausdrücklich als „methodisch sauber" freigegeben, **in Kenntnis beider Zahlen** — sie standen in der Anfrage. (2) Die Regel aus #45 ist einheitlich und definitorisch: Stufe 2 hat keinen freien Parameter, also gibt es nichts zu tunen; eine getunte Variante wäre begrifflich keine Stufe 2 mehr. (3) Der Befund des Strukturstrangs hängt nicht daran — auf dem Hold-out gewinnt die Baseline ohnehin (B-42), die Gesamtaussage „der Mehraufwand ist nicht belegt" steht unabhängig davon. **Preis, offen zu benennen:** Der Vorsprung in der Kreuzvalidierung ist gegen die schwächere von zwei vertretbaren Messlatten gemessen. Das gehört mit der Zahl 0,314 in Kapitel 8, nicht als Fußnote, sondern als Absatz | entschieden (Lukas), Vorbehalt in Kap. 8 |
| 47 | 2026-08-08 | **Schröter gibt Spezifikation, Baselines und Testkonstruktion frei — mit einer Auflage.** E-Mail vom 08.08.2026: „Ihre Klarstellungen zu Kapitel 8 und der einheitlichen Spezifikation über die Einwohnerzahl sind plausibel. Die Reduktion auf die einfacheren Varianten (Poisson und logistische Regression ohne Strafterm) ist methodisch sauber, vermeidet willkürliche Parameter und liefert zudem stärkere Vergleichswerte. **Dokumentieren Sie diese Begründung genau so.** Die methodischen Anpassungen bei den Tests sind ebenfalls korrekt. Die Reduktion auf zehn Werte zur Wahrung der Unabhängigkeit sowie die Holm-Bonferroni-Korrektur bei den multiplen Regressionsvergleichen sind sauber umgesetzt. Dass bei der Klassifikation ohne Korrektur gearbeitet wird, ist folgerichtig." | Damit sind **#43, #45, #37 und #38 abgestimmt**, und #35 ist in Bezug auf die Baselines überholt. Keine Festlegung der Modellierung ist mehr unabgestimmt. **Auflage D daraus:** Die Begründung der Baselines ist wörtlich in seiner Formulierung zu dokumentieren — methodisch sauber, vermeidet willkürliche Parameter, liefert stärkere Vergleichswerte (`01_VORGABEN.md`, Abschnitt 0). **Zu beachten:** Das dritte Argument trifft nur auf den Mengenstrang zu; im Strukturstrang sinkt die Latte von 0,314 auf 0,297. Beide Zahlen standen in der Anfrage, die Freigabe erfolgte in Kenntnis — in Kapitel 5.4 ist der Unterschied trotzdem zu benennen | erledigt; Auflage D in `01_VORGABEN.md` |
| 46 | 2026-08-07 | **Die Schlussbewertung umfasst beide Baselines.** `hold_out()` rechnet Stufe 1 und Stufe 2 mit, Spalte `stufe` in `holdout.csv` | Ohne Bezugspunkt ist die Schlussbewertung nicht interpretierbar: Die Primäraussage nach #34 vergleicht Verfahren gegen die Stufe-2-Baseline, und genau das sollte der Hold-out prüfen. Unbedenklich, weil beide Baselines keinen freien Hyperparameter besitzen — es gibt nichts, was der Blick auf den Hold-out hätte beeinflussen können | umgesetzt, `07_BEFUNDE.md` B-38 |
| 45 | 2026-08-06 | **Die beiden Stufe-2-Baselines sind unpenalisierte GLM mit kanonischem Link: Poisson mit Offset für die Menge, multinomiales Logit für die Struktur.** Ersetzt die Negative Binomial (#32) und die L2-penalisierte Logit. Keine der beiden hat einen freien Hyperparameter, also kein Tuning | Einheitliche Regel statt zweier Sonderfälle: Was einen freien Parameter hat, wird mit gleichem Budget getunt; was keinen hat, wird angepasst. Die Negative Binomial ist die Erweiterung für korrekte **Inferenz** — die Baseline liefert aber ausschließlich Punktvorhersagen, und dafür bleibt der Poisson-Schätzer bei richtig spezifiziertem bedingtem Mittelwert konsistent (Gourieroux, Monfort & Trognon 1984). Gemessen: Poisson 33,98 gegen Negative Binomial 37,27 RMSE — einfacher **und** besser. Der Strafterm der Logit war eine scikit-learn-Voreinstellung, keine Entscheidung; unpenalisiert ist die einfachste Form. **Folge, offen zu benennen:** Die Regressionslatte steigt von 37,27 auf 33,98, die Klassifikationslatte sinkt von 0,314 auf 0,297 — die Änderung hilft im einen Strang und schadet im anderen. **Schröter hat die Negative Binomial namentlich freigegeben (#35); der Wechsel ist ihm mitzuteilen** | umgesetzt, `07_BEFUNDE.md` B-36 |
| 44 | 2026-08-06 | **Die Stufe-2-Baseline der Klassifikation wird getunt** — `C` der logistischen Regression mit demselben Budget, demselben inneren CV (`GroupKFold(4)` nach Stadtteil) und demselben Scoring (`f1_macro`) wie die Vergleichsverfahren. Suchraum `C ∈ [1e-3, 1e3]`, symmetrisch zu Ridges `alpha`. Die Negative Binomial bleibt ungetunt — sie hat keinen freien Hyperparameter, α wird aus den Trainingsdaten geschätzt | `C` ist die Regularisierungsstärke und damit ein echter Hyperparameter. Blieb er auf dem Vorgabewert 1,0, während RF und XGBoost 50 Iterationen bekamen, verglich man ein ausgereiztes Verfahren mit einer nicht ausgereizten Messlatte — und ausgerechnet in diesem Strang schlagen die Verfahren die Baseline. Der Vorsprung wäre teilweise ein Artefakt der Tuning-Asymmetrie gewesen. **Gemessene Folge:** Macro-F1 der Baseline 0,298 → **0,314**, Macro-AUROC 0,711 → **0,757**. Der Vorsprung von Random Forest halbiert sich von +0,0296 auf +0,0136, der von XGBoost von +0,0362 auf +0,0203. Ob er signifikant bleibt, entscheidet der nächste `m03`-Lauf | **überholt durch #45** (kein Tuning, weil unpenalisiert kein freier Parameter); die Messung bleibt als Beleg gültig, `07_BEFUNDE.md` B-36 |
| 43 | 2026-08-06 | **Alle Verfahren modellieren die Rate und multiplizieren für die absolute Zahl mit der Einwohnerzahl zurück** (`AUF_RATE_MODELLIEREN` in `m02_menge.py`). Damit ist die Expositionsbehandlung zwischen Baseline und Vergleichsverfahren erstmals symmetrisch — die Negative Binomial verwendet diese Konstruktion über ihren Offset seit jeher. **Getroffen NACH dem zweiten Modelllauf**, in der Arbeit als solches zu kennzeichnen; der Lauf unter der alten Spezifikation bleibt vollständig berichtet | Die Forschungsfrage lautet, welches der drei **Verfahren** die höchste Prognosegüte erzielt. Verloren zwei davon, weil ihnen die Expositionsstruktur vorenthalten wurde, misst der Vergleich die Modellierungsentscheidung statt der Verfahren — die gestellte Frage bliebe unbeantwortet. Bei Zähldaten mit Expositionsgröße ist deren explizite Behandlung Standard; XGBoost bietet dafür sogar `base_margin`. Der Mechanismustest hat die Kosten der alten Spezifikation gemessen (B-33): Random Forest 67,7 → 36,4 RMSE, XGBoost 61,7 → 35,7, bei einer Baseline von 37,4. Der gesamte Rückstand von 24 bis 30 RMSE stammte aus der Spezifikation, nicht aus dem Verfahren. Fehler derselben Art wie der quadratische Verlust (#42). **Folge, offen zu benennen:** Aus „kein Verfahren schlägt die Baseline" wird voraussichtlich das Gegenteil — die Rangfolge kehrt sich auf Punktschätzerebene zu XGBoost > Random Forest > Ridge um. Ob signifikant, entscheidet der volle Lauf. **Der Unterschied zwischen beiden Spezifikationen ist selbst ein Ergebnis** und beantwortet UF4: Die Spezifikation zählt hier mehr als die Verfahrenswahl | umgesetzt, `07_BEFUNDE.md` B-33, B-34, R-9 |
| 42 | 2026-08-06 | **Die Baumverfahren bekommen eine zur Datenform passende Verlustfunktion.** XGBoost von `reg:squarederror` auf **`reg:tweedie`** mit getuntem Varianzexponenten (`tweedie_variance_power`, 1,1–1,9); Random Forest von `squared_error` auf **`criterion="poisson"`**. Ridge bleibt unverändert (`log(1+y)` leistet dasselbe). Nur die Regression, nicht die Klassifikation. **Getroffen NACH dem ersten Modelllauf** — in der Arbeit als solches zu kennzeichnen | Es war eine Ungleichbehandlung in der Spezifikation, kein Ergebnis über die Verfahren: Die Baseline rechnete mit einer Zähldaten-Likelihood und log-Verknüpfung, Ridge auf `log(1+y)`, die beiden Baumverfahren dagegen additiv mit quadratischem Fehler — bei Einsatzzahlen von 6 bis 280 und Dispersionsindex 62,8. Der quadratische Fehler gewichtet einen Fehler von 20 bei Tenderloin wie bei Seacliff, wo er das Dreifache des Gesamtwerts ausmacht. Es ist derselbe Gedanke, mit dem die Negative Binomial als Baseline begründet wurde (#32): die einfachste Form, die zur **Datenform** passt. Tweedie statt Poisson, weil die Varianz der Negative Binomial mit μ + αμ² wächst und Poisson (p = 1) bei diesem Dispersionsindex viel zu eng ist; der Exponent wird getunt statt willkürlich gesetzt. **Erwartung ausdrücklich offen:** Der Hauptgrund für das Zurückbleiben der Bäume dürfte die Extrapolation sein (R-3, 33,7 %), daran ändert die Verlustfunktion nichts | umgesetzt, `07_BEFUNDE.md` B-31 |
| 41 | 2026-08-06 | **Ridge bleibt im Vergleich — trotz der Messproblematik bei den Laufzeiten.** Erwogen und verworfen wurde, den linearen Arm zu streichen, weil er als einziges Verfahren nicht über Kerne skaliert | Drei Gründe: (1) Die Forschungsfrage nennt Ridge wörtlich; (2) ohne den linearen Arm bleiben zwei Baum-Ensembles — genau die Konstellation, mit der LightGBM und CatBoost verworfen wurden („vierte Variante derselben Familie ohne neue Frage"); (3) im Probelauf war Ridge auf `anzahl_einsaetze` das **beste** Verfahren (RMSE 37,9 gegen 64,6 und 57,3) — plausibel bei 33,7 % Extrapolation, weil Ridge linear weiterrechnet und Bäume den Randwert des letzten Blatts geben. Das Messproblem war lösbar (#40), das inhaltliche Loch wäre es nicht | umgesetzt |
| 40 | 2026-08-06 | **Modelle laufen einkernig, parallelisiert wird nur die Hyperparametersuche** (`N_JOBS_MODELL = 1`, `N_JOBS_SUCHE = -1`). Der Parallelisierungsgewinn wird getrennt erhoben (`train_sekunden_parallel`, `parallel_gewinn`), gemessen in Wiederholung 0 | Zwei Gründe. Praktisch: `RandomizedSearchCV(n_jobs=-1)` um einen Schätzer mit `n_jobs=-1` erzeugt verschachtelte Parallelität; die Prozesse blockierten sich so stark, dass ein Probelauf mit Budget 2 nach 15 Minuten noch in Phase 1 stand. Nach der Umstellung ist das Ridge-Tuning rund 14× schneller. Inhaltlich und wichtiger: Unterfrage 3 fragt nach dem Aufwand — misst man Ridge einkernig und die Ensembles über alle Kerne, steckt der Parallelisierungsgrad in derselben Zahl. **Belegt, dass die Rangfolge dadurch kippt:** mit `n_jobs=-1` lag Random Forest vor XGBoost (1,85 s gegen 3,33 s), einkernig ist XGBoost **12× schneller** (0,41 s gegen 4,88 s). Dass die Kernzahl nur die Dauer und nicht das Ergebnis ändert, prüft `ein_lauf()` per `assert` | umgesetzt, `07_BEFUNDE.md` B-10/B-16 |
| 39 | 2026-08-06 | **Der berichtete Aufwand ist der einkernige Rechenaufwand; die Skalierbarkeit über Kerne ist eine eigene Kennzahl** und beantwortet Unterfrage 4 mit | Zwei getrennte, saubere Aussagen statt einer unklaren. „Wie teuer ist ein Fit" ist maschinenunabhängiger als eine Wanduhrzeit unter Vollast; „wie gut skaliert das Verfahren" war vorher gar nicht messbar | umgesetzt |
| 38 | 2026-08-05 | **Zwei getrennte Testfamilien statt einer mit sieben Tests.** Holm-Bonferroni läuft in `m02` über 6 sekundäre Tests; der eine Test in `m03` bleibt unkorrigiert. Ersetzt die Festlegung „α/7" in `06_RISIKEN.md` R-10 | Regression und Klassifikation beantworten verschiedene Teilfragen; ein Zufallstreffer im einen Strang macht den anderen nicht falsch. Preis, offen zu benennen: Der Klassifikationsvergleich RF gegen XGBoost läuft gegen α = 0,05 statt gegen 0,0071. Bei der Regression ist der Unterschied gering (0,0083 statt 0,0071) | umgesetzt, in Kap. 7 zu benennen |
| 37 | 2026-08-05 | **Der Primärtest läuft auf den 10 Wiederholungsmitteln, nicht auf den 50 Einzelläufen.** Der Test über alle 50 bleibt als gekennzeichnete Sensitivität in derselben CSV (Spalte `teststufe`). Präzisiert #34 | Der gepaarte Wilcoxon setzt unabhängige Paare voraus; die 50 Läufe sind dieselben 29 Stadtteile in zehn Gruppierungen, seine p-Werte fielen zu klein aus. Dieselbe zweistufige Logik, die R-5 für die Streuung ohnehin verlangt. Erreichbarkeit geprüft: kleinstes zweiseitiges p bei n = 10 ist 0,00195 (also auch nach Holm erreichbar), bei n = 5 wären es 0,0625 und Signifikanz strukturell unmöglich. **Rest bleibt:** Auch die zehn Mittel sind nicht unabhängig, das Konfidenzintervall ist enger als die wahre Unsicherheit | umgesetzt, R-11 |
| 36 | 2026-08-05 | **Wiederholte Splits über `vorpruefung/v0_aufteilung.py` statt über den `versatz`.** Hold-out fest, doppelte Stratifizierung erhalten, je Wiederholung geseedete Mischung innerhalb der Rangblöcke. Die Baselines laufen ebenfalls über alle 10 Wiederholungen | Die dokumentierte Schleife war nicht durchführbar, am Datensatz nachgewiesen: (1) Der `versatz` rotiert nur die Gruppennummern — über 0–9 entstehen 6 Partitionen statt 10, bei festgehaltenem Hold-out sogar nur eine, und `std_wiederholungen` wäre exakt 0 gewesen. (2) Gruppe 0 **ist** das Hold-out; bei `versatz = 1` lag keiner der sechs Hold-out-Stadtteile mehr im Hold-out. (3) Ohne das Argument `selten` entfällt die Stratifizierung nach #30, 30 von 35 Stadtteilen wechseln den Fold. Wiederholung 0 reproduziert die Parquet-Dateien bitgenau, geprüft per `assert` bei jedem Aufruf | umgesetzt, `07_BEFUNDE.md` B-1 bis B-4 |
| 35 | 2026-08-04 | **Schröter gibt Stadtteil-Split und beide Baselines frei.** E-Mail vom 04.08.2026: „Der Stadtteil-Split ist für die von Ihnen formulierte Forschungsfrage methodisch gut begründet. […] Die Negative-Binomial-Regression und die multinomiale logistische Regression sind ebenfalls geeignete Baselines. […] Insofern können Sie wie geplant vorgehen." | Damit sind #29 (Stadtteil-Split), #32 und #33 (Baselines) abgestimmt und nicht mehr nur begründet. **Drei Auflagen daraus:** (A) Die Abweichung von der zeitreihengerechten Kreuzvalidierung ist **transparent zu erläutern** — das begründete Verwerfen in Kapitel 8 ist damit verlangt, nicht optional. (B) Die Zielsetzung der Validierung ist **wörtlich als „Generalisierung auf unbekannte Stadtteile"** zu formulieren; der Begriff gehört in Kapitel 5.4 und in die Zielsetzung. (C) Für alle Vergleichsmodelle sind **identische Merkmale und Splits** zu verwenden — erfüllt, aber im Text zu **belegen**: Die Fold-Zuteilung steht als Spalte in beiden Parquet-Dateien, die Merkmalsliste einmal in `prep/config.py`. Kein Modellskript kann davon abweichen; dieser Mechanismus gehört in Kapitel 5.4 beschrieben | erledigt; Auflagen in `01_VORGABEN.md` Abschnitt 0 |
| 28 | 2026-07-27 | **`prep/` weiter verdichtet:** s1 von 20 auf 14 Funktionen (643 → 597 Zeilen), s2 von 19 auf 13 (537 → 459). Konkret: die sechs DataSF-Ladefunktionen durch eine deklarative `QUELLEN`-Tabelle plus eine Schleife ersetzt; `quick_test` entfernt; `baue_merkmale` in `baue_regression` eingerückt; `bewerte_klassifikation`, `beste_schwelle` und `inneres_fenster_masken` gelöscht (null Aufrufer); `bewerte_mehrklassig` zu ihrem einzigen Aufrufer `m03_klassifikation.py` verschoben | Die sechs Ladefunktionen unterschieden sich nur in Ressourcen-ID, Feldauswahl und Spaltentypen — das ist eine Tabelle, keine sechs Funktionen. Die drei gelöschten Funktionen waren für den binären Robustheitslauf vorgesehen, der noch nicht existiert; sie werden mit ihm zusammen wieder geschrieben, dann passend zum tatsächlichen Bedarf. Alle Ausgaben byte-identisch | umgesetzt & validiert |
| 27 | 2026-07-27 | **Baselines zurück in die Data Preparation** als `prep/s3_baselines.py` (115 Zeilen, nur Zahlen, keine Abbildungen). `python prep/build.py` hat jetzt drei Schritte und endet mit den zwei Datensätzen **und** den Referenzwerten. Die Eignungsprüfung bleibt in `modelle/m01_eignung.py` und berichtet die Baselines nur noch. Zugleich entschieden: **keine vierte, explizit nichtlineare Baseline** | Auflage Schröter (27.07.2026): „Baseline in Data Preparation" und „bei nichtlinearen Zusammenhängen braucht man eine nichtlineare Baseline". Ersteres ist eine Strukturvorgabe — Repo und Gliederung folgen ihr jetzt beide (Kapitel 5.4 = `prep/s3`). Letzteres ist bereits erfüllt: Vormonatswert und saisonaler Durchschnitt sind **nichtparametrisch**, unterstellen also gar keine Funktionsform, und der Vormonatswert ist mit RMSE 17,78 zugleich die härteste Referenz. Von sechs Modellläufen schlägt sie nur Ridge (S+L). Begründung ausformuliert in `01_VORGABEN.md`, Abschnitt 0. Die Baseline ist kein Genehmigungspunkt, sondern ein Beweismittel — sie muss belegen, dass die komplexeren Verfahren besser sind. Genau deshalb muss sie fair sein: eine durch ihre Funktionsform benachteiligte Referenz würde den Beleg wertlos machen | **Struktur umgesetzt; die Begründung ist durch #29 und #32 überholt** — sie stützte sich auf den Vormonatswert, den es unter dem Stadtteil-Split nicht mehr gibt |
| 26 | 2026-07-27 | **Eignungsprüfung und Baselines aus `prep/` heraus nach `modelle/m01_eignung.py`.** `prep/` besteht jetzt aus `config.py`, `s1_daten.py`, `s2_datensaetze.py` und `build.py`; `python prep/build.py` endet mit den beiden finalen Datensätzen und tut sonst nichts mehr. Gleichzeitig die gesamte Dokumentation auf drei Dateien in `docs/` reduziert (Pipeline, Decision Log, Vorgaben); `ORIENTIERUNG.md`, `ABGABE.md`, `DATA_DICTIONARY.md` und sieben Dateien unter `docs/` entfallen (per Git wiederherstellbar) | Die Aufbereitung soll Daten erzeugen, sonst nichts. `s3_pruefung.py` erzeugte keine Daten, sondern einen Bericht und fünf Abbildungen – von seinen 769 Zeilen waren 255 reiner Berichtstext. Das ist wertvoll (Linearitätsprüfung ist Schröters harte Auflage, Gutachten R7), gehört aber vor die Modellierung und nicht in die Aufbereitung. Inhaltlich folgenlos: alle drei Parquet-Dateien, beide `baselines_*.csv` und der Eignungsbericht sind byte-identisch, 14/14 Prüfungen unverändert | umgesetzt & validiert |
| 23 | 2026-07-27 | **Lag-Vorlauf: Regression beginnt ebenfalls 2015-01** statt 2016-01 (`VORLAUF_MONATE = 12`). Aggregiert wird ab 2014-01, nach der Lag-Bildung wird auf 2015-01 zugeschnitten. **4.620 statt 4.200 Modellzeilen (+10 %)** | `lag_12` für Januar 2015 braucht Januar 2014 – der fehlte im Panel, also entfernte `dropna` das erste Jahr je Stadtteil. Das war unnötig: Die Lags brauchen ausschließlich `anzahl_einsaetze` aus der Vergangenheit, keine ACS-Merkmale. Der Grund für START = 2015 (Akademikerquote, #11) betrifft nur die Prädiktoren der Zielzeile. Zwei Vorteile: Regression und Klassifikation decken jetzt **denselben Zeitraum** ab, und das Training wird um 12 Monate länger. **Die Testfenster der drei Folds bleiben unverändert** (2022/2023/2024) – es kommt nur Trainingsmaterial hinzu, der Verfahrensvergleich bleibt strukturgleich. Kein Leakage: Die Vorlaufmonate gehen ausschließlich über `shift()` ein, nie als eigene Zeile (geprüft in `tests/`) | umgesetzt & validiert; **mit Schröter bestätigen** |

---

## Richtigstellungen

| Eintrag | Was falsch war | Richtigstellung | Stand |
|---|---|---|---|
| #13 (Exposure) | Als Beleg wurde ein Vorzeichenwechsel der **Armutsquote** genannt („+0,20 absolut, −0,13 je 1.000 Einwohner"). Auf dem aktuellen Datensatz stimmt das nicht: die Armutsquote korreliert **+0,47 absolut und +0,45 pro Kopf** — kein Vorzeichenwechsel. Die Zahl stammt vermutlich aus der Zeit vor dem Ausschluss der Parkgebiete (#19), als winzige Nenner die Raten verzerrten | Die **Bevölkerung** ist die Variable mit dem Vorzeichenwechsel: `log_bevoelkerung` **+0,32 absolut, −0,37 pro Kopf** (roh: +0,34 / −0,31). Die Entscheidung bleibt richtig, die Begründung lautet auf `log_bevoelkerung` | ✅ nachgerechnet 2026-08-03, hier korrigiert. **Im Eignungsbericht noch anzupassen** |

## Offene Punkte

| Punkt | Stand |
|---|---|
| ~~Nichtlineare Baseline (Auflage Schröter 27.07.)~~ | ✅ beantwortet durch #32: Negative Binomial ist auf der Originalskala nichtlinear und ein vollwertiger Gegner. Begründung in `01_VORGABEN.md`, Abschnitt 0 |
| ~~Mehrheitsklassen-Baseline für die Klassifikation~~ | ✅ `vorpruefung/v1_baselines.py`, seit #33 zweistufig |
| ~~Sensitivitätsanalyse „Einsätze je 1.000 Einwohner" (#13)~~ | ✅ erledigt durch #29 — die Rate ist seit dem Umbau eine vollwertige Zielgröße |
| ~~Schlagen die Modelle die Baseline überhaupt?~~ | ✅ **beantwortet, finaler Lauf 30./31.08. (#54).** In der Menge **kein einziger Primärvergleich signifikant** — Ridge liegt +2,06 RMSE vor dem Poisson-GLM (p = 0,275), Random Forest −1,98 (p = 0,922), XGBoost −4,40 (p = 0,275); signifikant sind nur die Verfahrensvergleiche untereinander (Ridge schlägt beide Bäume). In der Struktur schlägt **allein der Random Forest** das multinomiale Logit (+0,0173, p = 0,0039), XGBoost nicht (−0,0003, p = 1,0) — und auf dem Hold-out kehrt sich beides um. Zahlen in `03_STAND.md` §5 |
| ~~`modelle/m01`–`m03` neu schreiben~~ | ✅ `m02`, `m03`, `m04`, `m05` geschrieben und gelaufen |
| ~~Eignungsprüfung inkl. Linearitätsprüfung (R7)~~ | ✅ gerechnet, `results/eignungspruefung/` — **mit der Einschränkung aus B-41:** Die nachgewiesene Nichtlinearität generalisiert nicht, geprüft mit `v3_spezifikation.py` |
| ~~Randomized Search~~ | ✅ umgesetzt, Budget **100** je Verfahren und Fold seit #50, Protokoll in `results/*/tuning.csv` |
| ~~SHAP-Analyse~~ | ✅ `m04_shap.py`, blockweise nach Faktorgruppen; im Mengenstrang über die standardisierten Koeffizienten des GLM, weil kein Verfahren die Baseline schlägt. **Im finalen Lauf übersprungen `uebersprungen.csv` 7 von 8 Modellen** — `beitraege.csv` enthält allein struktur/random\_forest. Ein Attributionsvergleich zwischen den Baumverfahren ist damit nicht möglich und wird in §7.5 auch nicht behauptet |
| ~~End-Hold-out einmalig auswerten~~ | ✅ einmalig ausgewertet, beide Stränge, inklusive beider Baselines (#46) |
| ~~Abweichungen mit Schröter besprechen~~ | ✅ vollständig abgestimmt: #29/#32/#33 am 04.08. (#35), #31 am 03.08., #37/#38/#43/#45 am 08.08. (#47) |
| Binärer Robustheitslauf `ist_brand` | **verworfen** — ein zweiter Klassifikationsstrang verbreitert die Arbeit, ohne eine neue Frage zu beantworten (Gutachten R4/R8) |
| Robustheitsvariante mit Stadtteil-ID | **verworfen** — unter dem Stadtteil-Split ist die Stadtteil-ID im Test unbekannt und als Merkmal nicht verwendbar |
| Sensitivität „getunte Klassifikationsbaseline" | **verworfen am 08.08.2026** (#48) — nach Schröters Freigabe kein Muss; der Vorbehalt wird stattdessen in Kapitel 8 benannt |
| ~~**„Generalisierung auf unbekannte Stadtteile" in den Text**~~ | ✅ **erledigt 31.08.2026** — wörtlich in der Zielsetzung und in Kapitel 5.4 von `main.tex` (R-17) |
| ~~**Kapitel 6 bis 8 schreiben**~~ | ✅ **Fassung steht, 31.08.2026** — Kapitel 4 bis 8 auf den finalen Lauf gezogen, alle 21 Codeauszüge gegen den Quelltext geprüft |
| **KI-Verzeichnis in `main.tex` füllen** | **offen** — die Tabelle enthält zwei Platzhalter (`\enquote{...}`). Die Vorgaben verlangen dokumentierte KI-Nutzung |
| **`Quellen/` und die 250-MB-Grenze** | **offen** — der Ordner ist allein 248 MB groß (`01_VORGABEN.md`, Abschnitt 4) |
