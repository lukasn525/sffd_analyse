# Abhaengigkeiten der Arbeit - exakt gepinnt.
#
# WARUM EXAKT GEPINNT: Kapitel 6 behauptet Reproduzierbarkeit. Ohne feste
# Versionen ist das eine Behauptung - Baumverfahren koennen zwischen
# scikit-learn- und XGBoost-Versionen abweichen, und die Laufzeiten aus
# Unterfrage 3 sind ohnehin nur mit der gemessenen Umgebung zusammen lesbar.
#
# Stand 05.08.2026, abgeglichen mit dem venv im Repo (Python 3.14, Windows).
# Die frueheren Pins (pandas==2.1.4, pyarrow==15.0.0) waren seit dem Umbau
# falsch - installiert war bereits pandas 3.0.2.
#
#   python -m pip install -r requirements.txt
#   python -m pip freeze > requirements_lauf.txt      # nach dem finalen Lauf
#
# Die zweite Zeile ist kein Ritual: Sie haelt fest, was tatsaechlich lief,
# inklusive der transitiven Abhaengigkeiten.

# ── Kern ───────────────────────────────────────────────────────────────────
pandas==3.0.2
numpy==2.4.4
pyarrow==24.0.0
requests==2.33.1

# ── Modellierung und Diagnostik ────────────────────────────────────────────
# xgboost fehlte bisher in dieser Datei UND im venv, obwohl es eines der drei
# Vergleichsverfahren traegt (Decision Log #31). 3.4.0 liefert ein
# py3-none-win_amd64-Wheel und laeuft damit auch unter Python 3.14.
scikit-learn==1.8.0
scipy==1.17.1
statsmodels==0.14.6
xgboost==3.4.0

# ── Interpretation ─────────────────────────────────────────────────────────
# Fuer modelle/m04_shap.py. Ebenfalls bisher nirgends verzeichnet.
# 0.52.0 ist ein cp312-abi3-Wheel und damit auf 3.12 bis 3.14 lauffaehig.
shap==0.52.0

# ── Geodaten ───────────────────────────────────────────────────────────────
# Nur noetig, wenn die Caches crime_index_monatlich.csv bzw.
# land_use_2020_neighborhoods.csv in prep/s1_daten.py neu berechnet werden.
# Fuer Modellierung und Auswertung nicht erforderlich.
geopandas==1.1.3
shapely==2.1.2
pyogrio==0.12.1
pyproj==3.7.2

# ── Grafiken (modelle/m05_abbildungen.py) ──────────────────────────────────
matplotlib==3.10.9
seaborn==0.13.2

# ── Sonstiges ──────────────────────────────────────────────────────────────
openpyxl==3.1.5      # liest docs/FIR-0001_DataDictionary_fire-incidents.xlsx
